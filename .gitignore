# 构建产物
/tt.exe
/tt.exe~
*.exe
/dist/
# 前端产物不入库，但那个占位文件必须入库：main.go 的 //go:embed all:web/dist
# 在没有该目录时会直接报错（pattern all:web/dist: no matching files found），
# 于是全新克隆连 go build 都过不去。占位文件让"后端不依赖前端构建"成立。
# 注意：vite 的 emptyOutDir 会连它一起清掉，所以构建后由 postbuild 脚本写回
# （web/app/scripts/gitkeep.mjs）—— 只写在这个忽略规则里是不够的。
/web/dist/*
!/web/dist/.gitkeep
/web/node_modules/
/web/app/node_modules/
/desktop/node_modules/
/desktop/dist/

# 配置：含明文 SSH / 数据库口令，绝不入库
/config.json
/config.json.bak
/config.local.json
*/.tdev/
/srccache/
/debug-bps/
/logs/
.tt-serve.json
.tt-serve.log
.tdebug-serve.json
.tdebug-serve.log

# 本地数据
/erp_data.db
/erp_data.db-*

# 本机构建缓存
/.gocache/
/.gotmp/

# 编辑器与工具
/.zcode/
/.claude/skills/
/.claude/settings.local.json
.DS_Store
Thumbs.db

# 本机预览用的启动配置(含沙箱绝对路径),不入库
/.claude/launch.json

# 前端 check runner 的 esbuild 中间产物
/web/app/scripts/.tmp-*.mjs
