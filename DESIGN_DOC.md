# TT 项目结构与设计

本文只描述**大结构与核心关系**：项目由哪几块组成、它们之间怎么连接、边界划在哪里。
各目录内部的明细（每个包的职责、每条协议规则、每个参数）在各目录自己的说明里展开，本文不重复。

---

## 1. 是什么

TT 是一款**面向 Agent 的 CLI 开发工具**，用于开发基于 Genero BDL 技术栈的大型 ERP 系统 ——
鼎捷数智旗下的 T100。使用场景是：**人和 AI 一起改一套正在服役的 ERP 客制代码。**

它包含四块能力：

| 命令组 | 做什么 |
|---|---|
| `tt debug` | 经 SSH 驱动服务器上的 4GL 调试协议；本地 Web 调试工作台 + 命令行控制端 |
| `tt dev tzc` | 设计器**代码包**的编辑管线：导出成带围栏的 4GL 工作区，改完经三道闸门写回 |
| `tt dev tzs` | 设计器**表单包**的读写：由设计器自己的引擎执行，具名动词 + JSON 参数 |
| `tt dict` | ERP 数据字典查询：本地镜像与远程直查两条数据源 |

对外形态固定为四件事：**一个 Go 二进制 `tt`、一份 `config.json`、一个本地 Web 服务、一个 C# 引擎进程。**

### 1.1 两条设计红线

**红线一：不实现设计器的私有格式。**
`.tzc` 的行为依据设计器的公开发行物反推；`.tzs` 更彻底 —— 引擎直接加载**设计器自己的程序集**，
格式由设计器自己算。任何"我来解析 / 我来拼这个结构"的做法都违反这条。**能问的就不抄。**

设计器的程序集随仓库入库、随发行包分发，因此运行 tt 不需要另外安装设计器，也不需要配置它的路径。

**红线二：不破坏要能机械证明。**
凡是声称"安全"的地方，判据必须是可复现的产物或命令（逐字节哈希、回读内容、测试输出），
不是形容词。这条决定了多条管线的结构：写回前后的字节比对、往返不动点、评测装置不接受自述。

---

## 2. 运行时形态

### 2.1 三类进程

```
① tt <命令>              短命进程：一次调用，跑完退出。绝大多数命令都是这一类。
        │
        │ 需要常驻能力时，落到下面两类之一
        ▼
② tt serve               本地 Web 服务：常驻、单实例（一个配置目录一个）
        │
③ tzs-server.exe        .tzs 引擎守护进程：每工作区一个，命名管道 JSON-RPC
        ▼
   设计器的程序集（映射到引擎进程内）
```

| | 谁拉起 | 怎么找到它 | 怎么停 |
|---|---|---|---|
| `tt serve` | 自身后台自举，父进程立即返回 | 状态文件 | `tt serve --stop` |
| `tzs-server.exe` | Go 侧按需拉起 | **问引擎** | `tt dev tzs stop`；孤儿另有清理入口 |

三条与进程有关的核心关系：

- **单实例的判据是状态文件**，不是端口探测。同一配置目录下两个服务会争同一份状态文件，
  停止与自动寻址就会指向错误的实例。
- **服务地址由状态文件广播**：调试 API 挂在哪个前缀下是配置的一部分，控制端命令据此寻址，
  不假设对面是哪种装法。
- **就绪判据是连得上管道**，不是收到应答：引擎先完成引导再创建管道，且管道只允许一个服务端实例。

### 2.2 数据目录

**数据目录 = 配置所在目录。** 里面有两类东西，界限必须清楚：

- **唯一不可删的**：配置文件本身（含口令）。
- **运行状态与可再生数据**：会话状态文件、企业快照、源码镜像、日志与中断点等。
  "哪些算可再生"只在一处定义；往这个目录写新文件时必须归类，否则会被清理逻辑波及。

---

## 3. 仓库大结构

```
TT/
├─ main.go          内嵌前端产物并交给命令层。整个文件只做这一件事
├─ internal/        Go 侧全部能力（命令层、共享能力、四条能力线）
├─ web/             前端：一套 SPA + 一套共享设计件
├─ engine/          .tzs 引擎（C#）：自己的构建链、自己的产物目录
│  └─ designer/     设计器程序集（入库，随包分发）
├─ skills/          五套 AI 技能，同时是对外操作手册
├─ tools/           评测装置与打包辅助脚本
├─ installer/       安装包定义
├─ testdata/        测试夹具
└─ docs/            设计材料与第三方材料副本
```

`internal/` 内部按**层次**组织，只有四层：

| 层 | 目录 | 关系 |
|---|---|---|
| 入口与命令层 | 根、`cli/`、`cli/{debug,dev,dict}/` | 只有一个汇合点；三个命令组并列且互不依赖 |
| 共享能力层 | `config/`、`host/`、`dbconfig/`、`erpdb/`、`entdir/`、`output/`、`safesql/`、`sshtun/`、`winproc/`、`pathinstall/` | 被命令层与能力层共同依赖，自身不反向依赖业务 |
| 能力层 | `debug/`、`web/`、`dev/`、`dict/` | 各自独立，之间只通过注入相连 |
| 横向支撑 | `dev/tzs/` | 引擎客户端，依赖面刻意窄 |

`engine/` 内部只有两层：**纯文本/压缩容器层**（不反射、不碰 GUI 框架）与**反射管线层**
（引导、反射、会话、传输、函数表）。依赖单向，构建时校验分层未被破坏。

---

## 4. 分层与依赖关系

### 4.1 三条规矩

1. **三个命令组互不 import。** 共享能力一律下沉到共享层。
2. **往上通信靠注入，不让叶子反向 import。** 共享上下文住在叶子包，需要命令组提供的东西
   以函数变量注入。
3. **命令层只有一个汇合点**：统一服务、调试内核、字典动作三者的接线只发生在这一处。

### 4.2 依赖方向

```
命令层 ──┬─ 共享能力层（无内部依赖的叶子包是可替换基座）
         └─ 能力层 ── 共享能力层
```

两条不允许出现的方向：

- **能力层之间不互相 import。** 需要对方能力时，由命令层注入。
- **共享层不反向依赖业务包。** 配置层被依赖最广，但它自己只依赖叶子。

### 4.3 注入而非反向依赖

**接口声明在需要方，实现由接线处注入。** 这样既避免 import 环，也让能力层之间保持零耦合。

需要注入的东西分三类：**外部产物**（前端资源、版本号）、**跨层能力**（HTTP handler、
配置重载、数据源实现）、**平台差异**（管道客户端、进程原语）。

两处刻意的"不注入/不复用"同样是设计：会话状态文件与守护进程登记分开存放；守护进程管理
不复用后台服务管理的实现 —— 两者的探测方式、状态粒度、失败分类都不同，相同的只有
"起 / 判活 / 杀"三件原语，那部分已抽成独立包。

### 4.4 单一来源

同一件事**只保留一份实现**。出现第二份就会各自漂移，且漂移是静默的。这条规矩约束的关键项包括：
写配置、环境解析、账号取法、查询输出、口令打码、工作区落盘、引擎动词参数、引擎管道名、
语料根发现、危险字符与 SQL 的处理、"哪些目录算缓存"。

其中两条最具代表性：**引擎的动词参数以引擎自己的声明为唯一权威**（Go 侧不抄一份），
**引擎的管道名只能问引擎**（自己算错了会表现为"连不上"，与真实原因无关）。

---

## 5. 跨线共享的三件东西

四块能力都建立在这三件共享物之上，它们是整个项目的主干关系。

### 5.1 一份配置

- **一份文件、一个写入口**：所有写操作走同一条"读 → 校验 → 改 → 原子写"的路径，
  未知键一律保留（保存只影响声明要改的那一节）。
- **位置解析只有一份实现**，按优先级判定；**显式指定的路径就是答案**，不落到默认位置。
- **打码按名不按标记**：密码类键名一律遮，未知节里的未知键也会遮。
- 环境清单是**唯一的环境登记处**，其余各节只放工具参数。

### 5.2 一条链：登录 → 环境 → 企业 → 账号 → 库

```
配置里的环境
  ├─① SSH 登录
  ├─② 加载 T100 环境变量      → 路径类变量全部由登录区域派生
  ├─③ 企业编号                ← 这条链上唯一的"业务身份"
  ├─④ 查企业目录              企业编号 → 账号名
  └─⑤ 库地址                  来自配置，不由企业编号推导
```

**核心关系：企业编号决定的是账号（schema），不是库。** `topent` 与库的账号清单是两个独立字段，
不互相推导。T100 路径不允许静态配置，登录后动态获取是唯一权威来源。

**两条访问路径必须给出同一个账号**：一条在服务器侧执行（借用服务器上的工具），
一条由客户端直连（可选隧道）。二者共享的只有快照文件、指纹与新鲜期；各自怎么连、怎么查
由各自的层负责，互不搬用对方的能力。

### 5.3 一个出口

所有数据查询走同一个输出出口，以一份**落脚点元信息**说明"这次落在哪"（数据源、环境、账号与
账号来源、库、路由、只读标记、截断情况、落盘路径）。四条不许破：空段不打印；企业编号与账号
同行；截断绝不静默且结果绝不丢；stdout 只放数据。

错误同样是一个信封，错误码是**冻结契约**（能加不能改名），且 JSON 模式下错误也走 stdout。

---

## 6. 四条能力线的核心关系

### 6.1 调试：会话与事件的单一来源

调试内核持有一个会话池与一条事件总线。**会话事件是外部观察程序状态变化的唯一途径**，
界面与命令行都是从同一条总线取数，只是消费方式不同（一个长连接，一个短调用）。

会话本身是一台状态机，维护"命令-应答配对"。两条与人的边界：程序阻塞在交互语句时与空转
无法区分，因此有专门的探测动作；AI 与人对同一会话的操作权分开判定。

### 6.2 `.tzc`：唯一写路径 + 机械证明

三条公理构成这条线的骨架：**唯一编辑界面**（一个带围栏标注的 4GL 文件）、**唯一写路径**
（所有修改都走写回命令）、**不破坏是机械证明**（逐字节比对 → 不变量 → 装载模拟）。
围栏之外一个字节都不许动，写回前对新包做装载模拟，任何一步不过则原包不变。

两条与容器有关的关系：**只有被服务端消费的那个文件是设计文件**，其余是框架骨架或服务器产物，
各自的处理方式不同；**容器必须保持设计器写出的形态**，因此写回是字节级重建而非重新打包。

### 6.3 `.tzs`：引擎算，我们只驱动

**格式由设计器自己的程序集算，我们的引擎只是驱动层。** 对外暴露为一组动词，动词名与参数
全部来自引擎的运行时声明 —— 命令层没有动词字面量，引擎增改函数时 Go 侧不用改。

三条关系：**对外寻址用稳定的业务名**（会话标识是实现细节，不对外）；**校验是基线相对的**
（首次校验的那次运行本身就是基线，判断"是否改坏"必须先在同一个对象上建立基线）；
**通信严格串行**（设计器的活状态是进程全局且非线程安全的）。

### 6.4 字典：两个数据源，一个接口

本地镜像与远程直查实现**同一组查询方法、返回同一批结构、同语义同列序**，命令层只依赖那个接口，
数据源由配置策略与参数决定。方言差异集中在一处处理，不散到各命令。

同步是"写临时库 → 建索引 → 原子替换目标文件"，中途失败不影响原库。直连路径无绑定参数，
因此所有值经白名单后内联，并有库侧只读事务兜底。

---

## 7. Web 服务与前端

### 7.1 一个进程、一个端口、一套页面

| 路径 | 内容 |
|---|---|
| `/debug/` | 调试工作台单页应用 |
| `/debug/api/*` | 调试子系统 API |
| `/api/*` | 统一接口：配置、派生状态、安装、字典的长跑动作 |
| 其他 | 静态兜底到入口页；未知 API 路径回 JSON 错误，不落 HTML |

三条关系：**子系统只挂 API，页面统一由本层提供**（否则子系统的兜底路由会吃掉整个前缀）；
**两个子系统的端点命名空间有重叠，所以各占一个前缀**；**目录请求直接给入口页，未命中的路径
回落入口页**（否则刷新与深链接会失败）。

### 7.2 服务端替前端兜底

配置的校验归服务端（前端只负责展示中文说明）；保存时保留表单不管理的字段（代价是通过页面
删不掉它 —— 宁可选"删不掉"）；写入成功后通知持有配置内存态的子系统重新加载，否则会出现
"改了没生效"。

### 7.3 前端

一套单页应用 + 一套共享设计件。三个视图（调试、接口日志、服务测试）与统一设置页常驻挂载、
显隐切换；使用哈希路由；状态集中在一个容器里。

两条结构性关系：**语言工具是纯数据与纯函数**，不依赖编辑器组件，因此检查项可以在 Node 里跑，
不需要浏览器；**共享件在应用目录之外，其类扫描入口必须显式声明**，否则构建成功而界面缺样式
（这是静默的）。

**前端产物被后端内嵌**：产物目录里保留一个占位文件，使后端在没有前端产物时也能构建通过
（那样得到的二进制没有界面，服务返回一张写着构建命令的引导页）。

---

## 8. 构建与分发

### 8.1 三条互不相干的构建链

| 链 | 产物 | 何时跑 |
|---|---|---|
| 前端 | 前端产物目录 → 被后端内嵌 | 改了前端 |
| 后端 | 单个可执行文件 | 改了 Go |
| 引擎 | 引擎产物目录 | **只在真的改了引擎时** |

**引擎独立构建的三条理由**：它不属于后端的构建链（用系统自带的旧版编译器与 GUI 框架程序集）；
**重编会换掉程序集标识，而守护进程的命名包含它**，因此每次重建都会留下一批再无对应名字可用的
进程 —— 把它挂到后端构建上等于每次发版都制造一批；设计器程序集跟着产物走，所以引擎从源码树
直接跑、不需要任何环境变量。

### 8.2 三种布局

| 布局 | 配置落在哪 |
|---|---|
| 源码树 | 用户目录（或环境变量指定）；需指过去引擎路径与工作区 |
| 便携包 | 包内（带便携标记），解压即用，带引擎三件套与设计器目录 |
| 安装包 | 统一用户目录；用户级安装，免管理员，刻意不带便携标记与配置文件 |

**打包只采集、不构建**：引擎与设计器按名字采集（引擎产物目录里还有只供开发的探测程序，
不能整目录拷贝）；探测程序与引擎自带的客户端不进包 —— 对外只有 `tt` 一个入口。

---

## 9. 验证体系的骨架

**两层**：常规测试默认跑；全语料回归是**显式开关**（对上百个真实包各跑一遍，耗时十几分钟，
与默认超时冲突会产生随机器负载波动的假失败）。跑语料有两条纪律：**在副本上跑**（缺省语料根是
真实客户目录）、**整轮进行中不重建引擎**。

**判据是回读，不是自述**：说"改好了"之前，用一条能被别人重放的命令证明它。
已固定的判据样式包括：零改动重建与原包逐字节相同、往返不动点、"改动增量"而非绝对干净、
语料清单逐条比对。

**两套独立于单元测试的关卡**：一套是管线自带的内置对抗用例（不需要真实语料），
一套是工具面评测装置 —— 派只读技能文档的执行者做真事，再回读产物判分，不接受自述。
后者的结果按**摩擦**读，不按通过率读。

**最后一步由人做**：自动化只到"模拟装载"，真正的验收是在装了设计器的机器上打开产物。

---

## 10. 术语

| 词 | 含义 |
|---|---|
| **包 / 工作区** | 包 = 设计器的成品文件；工作区 = 导出出来的可编辑目录 |
| **围栏** | 圈出可编辑区间的单行注释标记；**不是内容，是坐标** |
| **区段 / 点** | 围栏圈出的单位：框架区段与自订定义点 |
| **句柄** | 引擎里一个已加载对象的会话标识，永不复用，随进程死亡失效 |
| **守护进程** | 引擎的常驻进程；命名含程序集标识，重编即换名 |
| **动词** | 引擎暴露的对外命令单位，由引擎自己的声明在运行时定义 |
| **基线** | 用来计算"增量"的参照点 |
| **闸门** | 写回前的机械检查，不过则原物不变 |
| **数据族** | 字典的多类数据集合，各自独立同步与判定完整性 |
| **落脚点** | 一次查询实际落在哪：数据源、环境、账号、库、路由 |
