TT 绿色（便携）版标记 —— 请不要删除本文件
================================================

有本文件在，TT 就把「配置 / 日志 / 断点 / 窗口位置」放在程序所在目录（exe 同目录），
与命令行便携包完全一致：

  config.json          SSH/区域/企业/数据库配置（含明文凭据，注意保护）
  logs\desktop.log     后端运行日志（超过 2MB 轮转一份 .1）
  .tt-serve.json       运行状态（pid/地址）；命令行也读它
  window-state.json    窗口位置
  debug-bps\           断点持久化

合并前这是 TDebug 的标记文件（原名 .portable 也一样），合并后它同时作用于
命令行版与桌面版 —— 因为两者用的是同一个 tt.exe 与同一套配置位置规则
（见 internal/config/paths.go 的 IsPortable）。

桌面版只是外壳：界面与 API 都来自随包的 tt.exe（内嵌前端）。
