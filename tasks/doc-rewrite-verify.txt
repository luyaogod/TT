文档重写工程（docs/WIKI.md → 每目录一份 README.md）的核实记录。
写哪份 README 时就地追加；结论落地后删掉已了结的条目。
工程背景与批次见 .claude/plans/ 下的计划文件。

================================================================================
【一】待验证（需要真机 / 客户环境 / 设计器 / 深度回归 / 未安装的依赖）
================================================================================

- WIKI §9.1 的十条验收数据（零改动 roundtrip 165/165、围栏可逆 165 包 73,107 Region、
  apply 仿真 137 包、.tzs 语料 67 包、对抗用例 31 项、TAP 恒等写 36,373 次…）
  | 想证实：这些数字今天是否仍成立
  | 怎么证实：TDEV_DEEP=1 go test ./... -timeout 30m（9–11 分钟）、
             TTZS_DEEP=1 go test ./internal/dev/tzs/ -run TestCorpus -timeout 30m（17 分钟）、
             ./tt.exe dev tzc selftest
  | 落在：待写进 internal/dev/* 各包的「判据」节（本批只写判据，不写数字）

- WIKI §6.16 与 §9.1 的语料统计自相矛盾：§6.16 说 <point>=32,678、<section>=3,646、
  CDATA 36,324 段；§9.1 说 32,571 / 3,641 / 36,212
  | 想证实：真实口径是哪个（或两者是不是同一口径）
  | 怎么证实：go test ./internal/dev/tapfile -count=1 -v（语料对拍），看它自己报的数
  | 落在：internal/dev/tapfile/README.md（批 1）

- testdata/fgl-fixtures/README.md（改写前）声称「166 个包、4,336 个自订点，4,335 个通过」
  本次实跑 go test ./internal/dev/fgl -count=1 -v 得到的是
  「ParseBlock(EnvelopeKindFor(点名前缀))：通过 3639 map[DIALOG:17 FUNCTION:3622]，失败 1」
  | 想证实：两个数是不是同一口径（子集 vs 全集），还是真的漂了
  | 怎么证实：读 internal/dev/fgl/corpus_test.go:248 附近的计数逻辑，确认它数的是什么集合
  | 落在：internal/dev/fgl/README.md（批 1）

- 前端侧夹具基线「用例 61 个：通过 58，已知偏差 3，不符 0」本次未跑
  | 原因：web/node_modules 不存在，npm run check:outline 跑不起来
  | 怎么证实：cd web && npm install && npm run check:app
  | 落在：web/app/scripts/fgl-fixtures/README.md（已写复现命令，未写数字）

- 真机验收（S7）：在装了设计器的机器上打开 `tt dev tzc apply` 产出的包，应无异常对话框、
  改动可见、保存后重开仍在
  | 需要：装了客户端 + 设计器的机器；输入必须是设计器产出的原生包
  | 落在：internal/dev/README.md（批 1）

- 三处 C# 注释指向 docs/WIKI.md（engine/src/Designer/{DryRun,Manifest,OpLog}.cs）
  | 计划：批 7 改注释 + 重编引擎 + reap 孤儿守护进程（重编换 MVID → 管道名变）
  | 注意：engine/ 下部分 C# 是 CRLF，改的时候别用文本模式批量替换


================================================================================
【二】已证伪 / 与代码不符（一律以代码为准，此处只记录）
================================================================================

- WIKI §1.2 的目录树画在合并前的路径上：把 pkgfile / tapfile / tglfile / fence / verify /
  split / synth / model 画在 internal/ 根下；实际都在 internal/dev/ 下。
  另外树里列出 internal/fgl、internal/atomic、web/debug、web/dict —— **这四个路径不存在**。
  | 依据：find . -type d 的目录清单

- WIKI §9.1 的复现命令用的是合并前路径，照抄跑不通：
    go test ./internal/pkgfile          → 实际 ./internal/dev/pkgfile
    go test ./internal/tapfile          → 实际 ./internal/dev/tapfile
    go test ./internal/fence            → 实际 ./internal/dev/fence
    go test ./internal/fgl              → 实际 ./internal/dev/fgl
    TDEV_DEEP=1 go test ./internal/cli -run TestCorpusExportVerify
                                        → 实际 ./internal/dev/cli（TestCorpusExportVerify 与
                                          TestCorpusApplySimulation 都在 internal/dev/cli/corpus_test.go）
    go test ./internal/model ./internal/cli -run …
                                        → 实际 ./internal/dev/model ./internal/dev/cli
  | 依据：grep -rn "func TestCorpus" internal --include=*_test.go

- testdata/fgl-fixtures/README.md（改写前）把消费者写成 internal/fgl 与
  internal/fgl/outline_fixtures_test.go；实际是 internal/dev/fgl/。
  | 依据：ls internal/dev/fgl/、grep -rn "fgl-fixtures" internal

- web/app/scripts/fgl-fixtures/README.md（改写前）引用 web/scripts/fgloutline.fixtures.mjs
  与 web/src/fgloutline.ts；实际是 web/app/scripts/fgloutline.fixtures.mjs 与
  web/app/src/fgloutline.ts。重新同步那段 PowerShell 也写的是 web\scripts\fgl-fixtures\。
  | 依据：ls web/app/scripts/、ls web/app/src/

- 根 README.md（本轮改写前）写「go test ./...  21 个包」；实际 35 个包，其中 22 个带测试文件。
  | 依据：go list ./... | wc -l = 35；go list -f '{{if or .TestGoFiles .XTestGoFiles}}X{{end}}' ./... 共 22 个 X

- testdata/fgl-fixtures/README.source.md 与 web/app/scripts/fgl-fixtures/README.md
  **逐字相同**（同一份 TDebug 侧原文被放了两处），且两处都带着上面那批过期路径。
  | 处理：批 0 把 source 那份压成纯溯源说明，另一份改写为面向前端侧的说明
  | 依据：cmp 两份文件

================================================================================
【三】本次已当场取到的数（写进 README 时引用，别外推）
================================================================================

- tapfile 语料对拍（2026-09-26 实测，go test ./internal/dev/tapfile -count=1 -v）：
  .tap=141 个、<point>=26,196、<section>=3,146、带 CDATA 元素=29,342、
  独立正则对拍通过 29,341（重名点 1 个）、恒等改写逐字节一致 141 包 / 29,482 次写操作
  | 注意：**WIKI 的两组数字都过时了** —— §6.16 写 32,678/3,646/36,324（105 包），
    §9.1 写 32,571/3,641/36,212/36,373 恒等写（165 包）；今天实跑都对不上。
    语料是活目录（人在里面干活），规模数字随时变 —— 所以 README 里只给命令、不写死数字。
  | 结论：**WIKI §6.16 与 §9.1 的 TAP 数字冲突，两方都不是今天的真值**
- internal/dev/cli 的 selftest：通过 31，失败 0（./tt.exe dev tzc selftest）
- `.tzc` 走 `tzs export` 会被挡住并指回 `tzc export`（退出码 2）—— 由 selftest 实测
  （不是照抄文档：见 selftest 输出"错误（退出码 2）：这是代码包…"）

- **`tt dev tzc apply` 的真实管线顺序**（读 cli.go 的 cmdApply 得到）：
  解析围栏 → gate1 → 源包未变校验（D-6）→ I15 拒绝 → split → gate2 → 改写字节 → gate3
  → 原子写 → git 提交
  | **WIKI §6.7 写的顺序是错的**：它写"gate1 → 源包未变校验 → gate2 → split → 原子写"，
    把 split 放在 gate2 之前还是之后写反了（代码里 split 先算计划，gate2 再判原包与文档的一致性）。
  | 已按代码写进 internal/dev/README.md 与 internal/dev/verify/README.md

- **代码注释里的「设计指南 §x.y」指向一份不在仓库里的文档**：全仓与 git 历史都找不到它
  （find/git log 皆空）。仓库里有的是 `docs/T100设计器-README.md`（第三方材料副本）与
  反编译源码的 file:line（源码树也不在仓库）。
  | 处理：新 README 只写条文本身 + 代码依据，不把「设计指南 §x」当可读文献引用；
    在 internal/dev/README.md 的「与外部材料的关系」里说明了这一点。

- **区段权限链 S1–S5 的实际归属是 `synth`**（synth.ResolveSection），不是计划里写的 `tglfile`。
  | 依据：grep ResolveSection 在 internal/dev/synth/synth.go:277 起
  | 已按代码把 S1–S5 写进 internal/dev/synth/README.md

- **§11.9 的"现状接口"逐条回代码核实的结果**（批 2，读 engine/src 与 internal/dev/tzs）：
  | 说法 | 核实结论 | 依据 |
  |---|---|---|
  | `save`/`field_add` 的 `out` 闸门 | **成立**，且是两条拒绝：指向任一已打开的包、落到工作区之外（后者会"写入成功但之后打不开"） | `engine/src/Designer/Fns/Session.cs` 的 `CheckOutPath` |
  | `save` 返回 `sha256` + `key` | **成立**，另有 `written`：dry-run 下 `written:false` 而 sha256 是"本该写入的字节"的摘要 | 同上，`Fns/Session.cs:287-305` |
  | `force` 已从 manifest 删掉 | **成立**（Manifest.cs 里没有它），但**函数体里的兜底拒绝还在** | `Fns/Session.cs:232` |
  | `E_NO_OP` 是成功码 | **成立** | `internal/dev/tzs/client.go` 的 `CodeNoOp`/`IsSuccessCode` |
  | `describe_kind` 支持 `kind:"layout"` 并报 type/values/initial | **成立** | `Manifest.cs:386`（`P.KindOrLayout`）、`Fns/Read.cs:479-480` |
  | `list_packages` 只列 .tzs/.tzv、不开包 | **成立** | `Fns/Read.cs:697-700` |
  | `dry_run` 不是靠撤销栈 | **成立**：渲染临时包 → 跑 → 重读；`reverted.mode` 可为 `none` | `engine/src/Designer/DryRun.cs` |
  | `reload` 的 baseline 三态 | **成立**：`kept`/`dropped`/`none`；`fileChanged` 也是三态，**null = 无法证明** | `Fns/Session.cs:596-618` |
  | `op` / `list_ops` 幂等日志 | **成立**，日志只在进程内、不落盘 | `engine/src/Designer/OpLog.cs` |
  | `field_add` 的 `added[]` 带绑定 | **成立**：返回 `added` / `validate` / `baselineCached`（存了包另给 `saved`） | `Fns/Session.cs:715-719` |
  | `store.ToolName` 仍是 `"tdev tzc"` | **成立**，且**全仓无任何代码读它**（唯一出现处就是写入点） | `internal/dev/store/store.go:62`、`store/manifest.go:143` |
  | 函数面覆盖为空（`gate-w3-fns.py` 起不来） | **成立**：它在运行时用 importlib 从路径加载 `gate-w3.py`，那份从未进仓库 | `engine/gate-w3-fns.py:71-73`、`engine/TASKS.md:23-27` |
  | `tt --config` 对 `tt dev` 需转成 `TT_CONFIG` | **成立**（含位置无关的两处写法都要摘） | `internal/cli/dev/run.go` 的 `takeRootFlags` |

- **两处代码注释仍用过时的 `--allow-sec` 说法**（该开关 v2 起已废弃，cli.go:469 保留它只为
  给出明确指引）：`verify/verify.go:10`（gate1 的权限类描述）与
  `split/split.go:72`（TglPatches 的条件）。实际机制是 `model.SectionState`（unlock 状态机）。
  | 处理：README 按**实际机制**写；这两处以代码为准后属于待清理的注释（不在本次范围）

- internal/dev/fgl 的夹具对拍：用例 61 个，通过 58，已知偏差 3，不符 0
  | 命令：go test ./internal/dev/fgl -count=1 -v
  | 三个已知偏差：dialog-subdialog（SUBDIALOG 未识别为节点）、
    input-by-name-record-star（标签丢 .*）、input-nested-semicolon（; 不作终止符）
- 两份夹具副本的 .4gl / .expected.json **逐字节相同**（只有 README 不同）
  | 命令：diff -rq testdata/fgl-fixtures web/app/scripts/fgl-fixtures
- 夹具规模：各 61 个 .4gl + 61 个 .expected.json
