# csc 产物。**必须在仓库内、且不在 dist/ 下**：
# build_portable.bat 第一步就是 `rmdir /s /q dist`，产物落在那里会被自己删掉。
/out/

*.exe
*.dll
*.pdb
*.obj

# 例外：designer/ 是**入库**的 —— 它是引擎运行期的依赖（Assembly.LoadFrom 它），
# 随仓库一起分发，clone 下来就能跑 .tzs。上面那几条扩展名规则是拦 csc 产物的，
# 别把它们一起拦掉。见 engine/BUILD.md。
!designer/
!designer/*.dll

# 关卡与批脚本的临时包。它们本来就落在工作区（D:\t100_wrok_dir），这是防御。
_ai*.tzs
_zz*.tzs

# gate-w3-fns.py 把 gate-w3.py 当模块 import，Python 会写这个。
__pycache__/
